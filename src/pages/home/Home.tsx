import Hero from "../../components/hero/Hero";

const Home = () => {
  return (
    <main className="h-[calc(100vh-68px)]">
      <section className="h-full">
        <Hero />
      </section>
    </main>
  );
};

export default Home;
